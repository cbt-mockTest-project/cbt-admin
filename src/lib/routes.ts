export const MockExamRoutes = {
  write: '/mockExams/write',
  detail: (id: number) => `/mockExams/${id}`,
  writtendTest: '/mockExams/writtendTest',
};

export const authRoutes = {
  login: '/auth/login',
  register: '/auth/register',
};
